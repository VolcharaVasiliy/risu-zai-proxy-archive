# Сессия 11.08.2026: статус провайдеров и план

## Что сделано

### Gemini Web — РЕШЕНО (работает через Vercel)
- Диагностика: `BardErrorInfo [1060]` = `IP_TEMPORARILY_BLOCKED` (источник: `HanaokaYuzu/Gemini-API` constants.py:286).
- Повтор дампа браузера 1-в-1 (тело StreamGenerate, `at`, `f.sid`, все `x-goog-ext-*`) → всё равно 1060. Куки валидны (залогиненный HTML), но SNlM0e в HTML больше нет даже у залогиненного пользователя (upstream issue #297, PR #310).
- Вывод: это геоблок локального IP, не проблема токена.
- Проверка с IP Vercel: `POST /v1/chat/completions` model=`gemini-web` → **HTTP 200 «Hello»**. Геоблок подтверждён, SNlM0e не нужен.
- Добавлен override-токенов в `py/gemini_web_proxy.py`:
  - `GEMINI_WEB_AT_TOKEN` — форсировать `at` (заменить SNlM0e), значение из дампа: `ADR5zapNbKkoFvimRLHLHLMeMdsl:1786389731018`
  - `GEMINI_WEB_F_SID` — форсировать `f.sid`, из дампа: `132243216987997177` (без него → HTTP 400 `["er",...,400]`)
  - `GEMINI_WEB_LANGUAGE` — форсировать язык (`ru`)
- Ошибка 1060 уже мапится: `_error_from_code` → «Gemini Web rejected this account, region, or IP (status 1060)» (gemini_web_proxy.py:695).

### Perplexity — РЕШЕНО (работает)
- Раньше: `FREE_TIER_RATE_LIMITED` (честная ошибка лимита free-тарифа).
- Правка (коммит bea5f58): модель Turbo, `x-pplx-account` (`_pplx_account_id()`), `x-perplexity-request-endpoint`/`request-reason`, ru-RU/Europe/Moscow.
- Проверка с IP Vercel: model=`Turbo` → **HTTP 200 «Hi!»**. Лимит ушёл.

### Qwen — РЕШЕНО (ранее)
- Без Authorization (токен в куке), YaBrowser 26.3, bx-UA/UMID replay — работает локально и на Vercel.

### Деплой (scripts/redeploy-vercel.ps1)
- Скрипт падал «Cannot bind ... empty string»: PS 7.6 не биндит `[string[]]` если в массиве пустые строки, а `$env:NODE_EXE`/`$env:PYTHON` были пустыми.
- Фикс: создать `path-config.json` (node/python executable) + задать `$env:NODE`, `$env:NODE_EXE`, `$env:PYTHON`, `$env:PYTHON_EXE` перед запуском.
- Production: `https://risu-zai-proxy-archive.vercel.app`
- Env на Vercel: `GEMINI_WEB_AT_TOKEN`, `GEMINI_WEB_F_SID`, `GEMINI_WEB_LANGUAGE=ru` (добавлены 11.08.2026).

### Mimo — в работе
- Локально: 502 (DNS подмена → `127.0.0.1`, `_loopback_resolution`); с обходом DNS (RESOLVE на публичные IP) соединение не идёт.
- На Vercel: запрос **проходит** (DNS чистый), но upstream возвращает SSE-ошибку — текст был «?????,?????» (китайская кодировка ломалась при декоде utf-8 with ignore).
- Фикс (коммит e88e266): `_mimo_decode` — пробуем utf-8 → gbk → gb18030 → big5; `_iter_sse_events` читает байты (`decode_unicode=False`).
- **НЕ ЗАДЕПЛОЕНО**: Vercel free лимит 100 деплоев/день исчерпан (`api-deployments-free-per-day`), ближайший деплой через ~24ч.

## Закоммичено и запушено (main → GitHub VolcharaVasiliy/risu-zai-proxy-archive)
- `bea5f58` — YaBrowser 26.3 UA, perplexity turbo + x-pplx-account, openai oai-did session, fresh captcha creds
- `c76ab3a` — gemini-web: force at token и f.sid через env
- `e88e266` — mimo: decode SSE utf-8/gbk/gb18030

## Осталось сделать (по приоритету)

1. **Mimo** (после сброса лимита Vercel, ~24ч):
   - `scripts/redeploy-vercel.ps1 -Target production` (env: NODE/NODE_EXE/PYTHON/PYTHON_EXE)
   - Проверить `model=mimo-v2-flash-studio` → теперь текст ошибки читаемый: геоблок Китая (Vercel не подойдёт) или другая причина.
   - Если регион-блок — обсудить: китайские регионы/VPS/Cloudflare-воркеры в Китае.

2. **openai-web** — на Vercel `502 Turnstile verification`:
   - Нужен свежий `x-turnstile-token` из дампа ChatGPT (данные-рельные/чатгпт-*) и/или отдача/паузка токена через `x-turnstile-token` заголовок в прокси.
   - Проверить `Oai-Language/device-id/oai-did` уже в правках bea5f58.

3. **grok** — `403 Request rejected by anti-bot rules` (даже с Vercel, не геоблок):
   - Искать свежий `cf_clearance`/`GROK_CF_CLEARANCE` из дампа, имитировать полный fingerprint (JA3, headers).
   - YaBrowser 26.3 UA уже установлен.

4. **arcee** — `401 Session expired` (дамп сети пуст): нужен свежий экспорт сессии arcee.
5. **phind** — `401`: нужен свежий экспорт кук.
6. **mimo** локально — DNS-подмена (хост указывает на 127.0.0.1): обход через `MIMO_RESOLVE_IPS` уже есть; протестировать с реальным IP из dns.google.
7. Не трогаем (договорились): провайдеры, которые уже работают (qwen и т.д.).

## Полезные команды

```powershell
# Деплой (после задания env):
$env:NODE='F:\DevTools\Portable\NodeJS\node.exe'
$env:NODE_EXE=$env:NODE
$env:PYTHON='F:\DevTools\Python311\python.exe'
$env:PYTHON_EXE=$env:PYTHON
.\scripts\redeploy-vercel.ps1 -Target production -SyncEnv

# Проверка провайдера на Vercel:
# POST https://risu-zai-proxy-archive.vercel.app/v1/chat/completions
# Authorization: Bearer zai-session-secret
# { "model": "gemini-web" | "Turbo" | "mimo-v2-flash-studio" | "grok-3", "messages": [{"role":"user","content":"..."}], "stream": false }
```